const HighLightText = ({ children }: any) => {
  return (
    <span className="bg-primary/20 px-2 py-[1px] rounded-[4px] shadow-sm font-medium">
      {children}
    </span>
  );
};

export default HighLightText;
